# Claude Code Setup

Install the package globally or run it through your package manager, then add
Across Context as a local MCP server:

```bash
across-context install claude-code --stdout
```

Run the printed command. After that, ask Claude Code to search or remember
context through the `across-context` MCP tools.
